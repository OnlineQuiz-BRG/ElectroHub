
{
  "name": "electrohub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html index.tsx App.tsx types.ts mockData.ts metadata.json vercel.json dist/ && cp -r components services dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "react-router-dom": "^7.1.3",
    "lucide-react": "^0.474.0",
    "recharts": "^2.15.0",
    "@google/genai": "^1.3.0"
  }
}
